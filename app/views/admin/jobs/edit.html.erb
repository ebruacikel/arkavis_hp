<h1>Editing job</h1>

<%= render :partial => 'form', :locals => {:send_to_url => admin_job_path(@job)} %>

<%= link_to 'Show', admin_job_path(@job), :class => "btn" %>
<%= link_to 'Back', admin_jobs_path, :class => "btn" %>
