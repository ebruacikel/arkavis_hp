<h1><%= @page.title %></h1>
<p>
  <b>Body:</b>
  <%= raw(@page.body) %>
</p>

<p>
  <b>Weight:</b>
  <%= raw(@page.weight) %>
</p>
<%= link_to 'Edit', edit_admin_page_path(@page), :class => "btn" %>
<%= link_to 'Back', admin_pages_path, :class => "btn" %>