<div><p id="notice"><%= notice %></p></div>

  <div><h2><%= @post.title %></h2></div>

	<div><p><%=  @post.created_at.strftime("%B %d, %Y")%></p></div>
 
  
 	 <div class="post-body">
      <p><%= raw simple_format(@post.body) %></p>
	 </div>
  


  	<div>
	  <%= link_to 'Edit', edit_admin_post_path(@post), :class => "btn" %>
	  <%= link_to 'Back', admin_posts_path, :class => "btn" %>
	</div>